#include <cstdio>

int main(){
	FILE * in = fopen("spiesin.txt","r");
	FILE * out = fopen("spiesout.txt","w");
	
	
	
	int j;
	fscanf(in,"%d",&j);
	int a[j], b[j];

	for(int i=0; i<j; i++){ fscanf(in,"%d %d",&a[i], &b[i]);}
	fscanf(in,"%d",&g);
	int c[g],d[g];
	for(int i=0; i<g; i++){fscanf(in,"%d %d",&c[i], &d[i]);}
	for(int i=0; i<g; i++){
		
		}
		
	}
	 